{
  "ReverseProxy": {
    "_security_note": "PRODUCTION: Use HTTPS for all cluster destinations. Real addresses should be injected via environment variables for security.",
    "Clusters": {
      "user-cluster": {
        "Destinations": {
          "primary": { "Address": "https://user-api-service.prod.cluster.local" }
        }
      }
    }
  }
}
