{
  "ReverseProxy": {
    "_comment": "STAGING: Traffic is routed to internal staging endpoints. Ensure 'Address' is reachable from the Gateway container.",
    "Clusters": {
      "user-cluster": {
        "Destinations": {
          "primary": { "Address": "https://user-api.staging.internal" }
        }
      }
    }
  }
}
